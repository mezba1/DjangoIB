## Running Locally

1. Create a `.env` file based off `.env.sample` at the root of project directory.

```..env
$ cp .env.sample .env
```

Edit and make neccessary modifications to `.env`.

2. Run with docker.

```bash
$ docker-compose up
```
